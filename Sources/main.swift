import Cocoa

@main
struct LabelLinkApp: App {
    var body: some Scene {
        Window("Label 和 Link", id: "main") {
            ContentView()
        }
        .defaultSize(width: 500, height: 450)
    }
}
